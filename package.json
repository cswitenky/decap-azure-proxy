{
	"name": "decap-azure-proxy",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"build": "tsc && cp host.json dist/ && mkdir -p build && cp index.html build/",
		"dev": "func start",
		"start": "func start",
		"test": "vitest"
	},
	"devDependencies": {
		"@azure/functions": "^4.0.0",
		"@types/node": "^20.0.0",
		"typescript": "^5.0.4",
		"vitest": "1.3.0"
	},
	"dependencies": {
		"@azure/functions": "^4.0.0"
	}
}
