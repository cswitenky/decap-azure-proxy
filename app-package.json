{
  "name": "decap-azure-proxy-app",
  "version": "1.0.0",
  "description": "Static web app for Decap Azure Proxy",
  "scripts": {
    "build": "mkdir -p dist && cp index.html dist/",
    "start": "echo 'Static app ready'"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
